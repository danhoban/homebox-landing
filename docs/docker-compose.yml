services:
  homebox-landing:
    image: ghcr.io/danhoban/homebox-landing:latest
    restart: unless-stopped
    ports:
      - "127.0.0.1:8080:8080"
    volumes:
      # Mount your .env file into the container
      - ./.env:/app/.env:ro
